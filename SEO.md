# SEO Notes

The current canonical site URL is:

https://www.aristote.dev/

When a custom domain is connected, update this URL in:

- `index.html`
- `public/robots.txt`
- `public/sitemap.xml`

After deployment, submit `https://your-domain/sitemap.xml` in Google Search Console and use URL Inspection to request indexing for the homepage.
